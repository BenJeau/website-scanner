[project]
name = "website-scanner"
version = "0.1.0"
description = "a simple website scanner using botasaurus"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["botasaurus>=4.0.88", "faker>=37.4.0"]
